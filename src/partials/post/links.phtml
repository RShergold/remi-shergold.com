<div class="PostLinks">
  <p class="PostLinks-title">
    more posts
  </p>
  <? foreach(Nav::pages_in_current_section() as $page): ?>
    <a class="PostLinks-link <? if ($page->is_current()) echo 'is-current' ?>" href="<?= $page->src() ?>">
      <?= $page->title ?>
    </a>   
  <? endforeach; ?>
</div>